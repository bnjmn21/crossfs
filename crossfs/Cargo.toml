[package]
name = "crossfs"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.34.0"
ipc-channel = { workspace = true }
serde = { workspace = true }
simconnect = { version = "0.4.0", optional = true }

[features]
default = ["msfs"]
msfs = ["dep:simconnect"]
